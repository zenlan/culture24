<?php

class culture24_views_handler_argument extends views_handler_argument {

  function construct() {
    parent::construct();
  }

  function init(&$view, &$options) {
    parent::init($view, $options);
  }

  function query($group_by = FALSE) {
  }

}